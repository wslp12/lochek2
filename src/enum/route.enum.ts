const enum ROUTE_PATH {
  MAIN = '/main',
  CHAR_SETTING = '/char-setting',
  RAID_INFO = '/raid-info',
  USERS = '/users',
  LOGIN = '/login',
  SCHEDULE = '/schedule',
}

const enum ROUTE_PARAM {
  USER_ID = 'userId',
}

export { ROUTE_PATH, ROUTE_PARAM };
